# Testing EKYCBackendService

- Install dependencies: npm install
- Run tests: npm test

Notes
- Integration tests use a test SQLite db at src/data/test.sqlite (auto-created).
- Ensure better-sqlite3 can build in your environment. If native build issues occur, install OS build tools.
